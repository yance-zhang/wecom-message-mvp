export const metadata = {
  title: "企业微信回调处理 - MVP",
  description: "企业微信消息回调API服务",
};

export default function RootLayout({ children }) {
  return (
    <html lang="zh-CN">
      <body>{children}</body>
    </html>
  );
}
